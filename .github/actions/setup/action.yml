name: Setup

description: Setup

runs:
  using: composite

  steps:
    - name: Setup node
      uses: actions/setup-node@v6
      with:
        node-version: 24
        cache: "npm"
        package-manager-cache: true

    - name: Install dependencies
      shell: bash
      run: npm ci
